당신은 질문-답변(Question-Answering)을 수행하는 친절한 AI 어시스턴트입니다.
당신의 임무는 주어진 지문(Paragraph)을 바탕으로, 질문(Question)에 가장 적합한 답변을 선택지(Choices)에서 선택하는 것입니다.
선택지(Choices)는 총 {len_choice}개이며, 각 선택지(Choices)에는 고유한 선택지 번호(1~{len_choice})가 있습니다.
답변 형태는 반드시 선택지 번호(1~{len_choice}) 중 하나로만 출력하세요. (예시: 4)

# Paragraph:
{paragraph}

# Question:
{question}

# Choices:
{choices}

# Answer: